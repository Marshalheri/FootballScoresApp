import React from 'react';
import {myHeader} from '../public/Css/main.css'

function Header () {
  return (
    <div className={myHeader}>
      <h1 className={myHeader}>This is the header</h1>
    </div>
  );
};

export default Header;
